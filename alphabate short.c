#include<stdio.h>
#include<conio.h>
#include<string.h>
//void sort(cgar);
void main()
{
    char a[30],t,b[30];
    int i,j,k,l;
    //sort(a);
    printf("Enter the name");
    gets(a);
    k=strlen(a);
   // for(i=0;i<=k;i++)
    //{
       // for(j=i+1;j<=k;j++)
        //{
            //l=strcmp(a(i),a(j))
           // if(l>0)
           // t=a(i);
           // a(i)=a(j);
           // a(j)=t;
        //}
      //  printf("%s",a[i]);
    //}
    puts(a);
    getch();
}
